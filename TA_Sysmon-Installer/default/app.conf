[launcher]
author = Mohammad Mirasadollahi (https://github.com/Mohammad-Mirasadollahi)
description = Manages the Sysmon service lifecycle (install, uninstall, upgrade). The target version is detected automatically from the bundled bin/Sysmon.exe.
version = 2.3.0

[ui]
is_visible = 0
label = Sysmon Installer/Service Management TA

[package]
id = TA-Sysmon-Installer

[install]
is_configured = true
