<Sysmon schemaversion="4.90">
  <!-- Minimal bootstrap configuration: log process activity only. -->
  <!-- This is only the default applied at first install. The authoritative -->
  <!-- production config is owned by TA-Sysmon-Config. -->
  <EventFiltering>
    <!-- Event ID 1: log every process creation (empty "exclude" excludes nothing). -->
    <RuleGroup name="ProcessCreate" groupRelation="or">
      <ProcessCreate onmatch="exclude" />
    </RuleGroup>

    <!-- Event ID 5: log every process termination. -->
    <RuleGroup name="ProcessTerminate" groupRelation="or">
      <ProcessTerminate onmatch="exclude" />
    </RuleGroup>
  </EventFiltering>
</Sysmon>
