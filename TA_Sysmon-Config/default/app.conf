[launcher]
author = Mohammad Mirasadollahi (https://github.com/Mohammad-Mirasadollahi)
description = Deploys the authoritative Sysmon configuration (config.xml) and applies it to the running service.
version = 1.2.0

[ui]
is_visible = 0
label = Sysmon Configuration Management TA

[package]
id = TA-Sysmon-Config

[install]
is_configured = true
